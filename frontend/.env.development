# Talk to the real API by default, so following the README quick start shows your
# actual knowledge base rather than sample data that merely looks like it worked.
# For frontend work without a backend running, set VITE_USE_FIXTURES=true in
# .env.development.local (gitignored).
VITE_USE_FIXTURES=false
VITE_API_BASE=/api
